================================================================================
DATABASE CREDENTIALS FOR: {{ item.domain }}
================================================================================

Database Name: {{ item.db_name }}
Database User: {{ item.db_user }}
Database Password: {{ domain_db_passwords[item.db_name] | default('UNDEFINED') }}
Database Host: localhost

================================================================================
CONNECTION STRING EXAMPLE (PHP):
================================================================================

$host = 'localhost';
$dbname = '{{ item.db_name }}';
$user = '{{ item.db_user }}';
$password = '{{ domain_db_passwords[item.db_name] | default('UNDEFINED') }}';

try {
    $pdo = new PDO("mysql:host=$host;dbname=$dbname;charset=utf8mb4", $user, $password);
    $pdo->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);
} catch(PDOException $e) {
    die("Connection failed: " . $e->getMessage());
}

================================================================================
COMMAND LINE ACCESS:
================================================================================

mysql -u {{ item.db_user }} -p {{ item.db_name }}

================================================================================
IMPORTANT SECURITY NOTES:
================================================================================

1. Keep this file secure - it contains sensitive database credentials
2. Never commit these credentials to version control
3. Use environment variables in production applications when possible
4. Regularly rotate database passwords
5. This file is readable only by your user account

================================================================================
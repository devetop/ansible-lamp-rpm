================================================================================
DATABASE CREDENTIALS FOR: {{ item.1.domain }}
================================================================================

Database Name: {{ item.0.username }}_{{ item.1.domain | regex_replace('[^a-zA-Z0-9]', '_') }}
Database User: {{ item.0.username }}_{{ item.1.domain | regex_replace('[^a-zA-Z0-9]', '_') | truncate(16, True, '') }}
Database Password: {{ domain_db_passwords[item.0.username + '_' + item.1.domain] }}
Database Host: localhost

================================================================================
CONNECTION STRING EXAMPLE (PHP):
================================================================================

$host = 'localhost';
$dbname = '{{ item.0.username }}_{{ item.1.domain | regex_replace('[^a-zA-Z0-9]', '_') }}';
$user = '{{ item.0.username }}_{{ item.1.domain | regex_replace('[^a-zA-Z0-9]', '_') | truncate(16, True, '') }}';
$password = '{{ domain_db_passwords[item.0.username + '_' + item.1.domain] }}';

try {
    $pdo = new PDO("mysql:host=$host;dbname=$dbname;charset=utf8mb4", $user, $password);
    $pdo->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);
} catch(PDOException $e) {
    die("Connection failed: " . $e->getMessage());
}

================================================================================
COMMAND LINE ACCESS:
================================================================================

mysql -u {{ item.0.username }}_{{ item.1.domain | regex_replace('[^a-zA-Z0-9]', '_') | truncate(16, True, '') }} -p {{ item.0.username }}_{{ item.1.domain | regex_replace('[^a-zA-Z0-9]', '_') }}

================================================================================
IMPORTANT SECURITY NOTES:
================================================================================

1. Keep this file secure - it contains sensitive database credentials
2. Never commit these credentials to version control
3. Use environment variables in production applications when possible
4. Regularly rotate database passwords
5. This file is readable only by your user account

================================================================================

================================================================================
DATABASE CREDENTIALS FOR: {{ item.2 }}.{{ item.1.domain }}
================================================================================

Database Name: {{ item.0.username }}_{{ item.2 }}_{{ item.1.domain | regex_replace('[^a-zA-Z0-9]', '_') }}
Database User: {{ (item.0.username + '_' + item.2 + '_' + item.1.domain) | regex_replace('[^a-zA-Z0-9]', '_') | truncate(16, True, '') }}
Database Password: {{ subdomain_db_passwords[item.0.username + '_' + item.2 + '_' + item.1.domain] }}
Database Host: localhost

================================================================================
CONNECTION STRING EXAMPLE (PHP):
================================================================================

$host = 'localhost';
$dbname = '{{ item.0.username }}_{{ item.2 }}_{{ item.1.domain | regex_replace('[^a-zA-Z0-9]', '_') }}';
$user = '{{ (item.0.username + '_' + item.2 + '_' + item.1.domain) | regex_replace('[^a-zA-Z0-9]', '_') | truncate(16, True, '') }}';
$password = '{{ subdomain_db_passwords[item.0.username + '_' + item.2 + '_' + item.1.domain] }}';

try {
    $pdo = new PDO("mysql:host=$host;dbname=$dbname;charset=utf8mb4", $user, $password);
    $pdo->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);
} catch(PDOException $e) {
    die("Connection failed: " . $e->getMessage());
}

================================================================================
COMMAND LINE ACCESS:
================================================================================

mysql -u {{ (item.0.username + '_' + item.2 + '_' + item.1.domain) | regex_replace('[^a-zA-Z0-9]', '_') | truncate(16, True, '') }} -p {{ item.0.username }}_{{ item.2 }}_{{ item.1.domain | regex_replace('[^a-zA-Z0-9]', '_') }}

================================================================================

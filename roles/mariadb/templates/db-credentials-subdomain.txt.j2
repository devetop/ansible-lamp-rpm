================================================================================
DATABASE CREDENTIALS FOR: {{ item.subdomain }}.{{ item.domain }}
================================================================================

Database Name: {{ item.db_name }}
Database User: {{ item.db_user }}
Database Password: {{ subdomain_db_passwords[item.username + '_' + item.subdomain + '_' + item.domain] }}
Database Host: localhost

================================================================================
CONNECTION STRING EXAMPLE (PHP):
================================================================================

$host = 'localhost';
$dbname = '{{ item.db_name }}';
$user = '{{ item.db_user }}';
$password = '{{ subdomain_db_passwords[item.username + '_' + item.subdomain + '_' + item.domain] }}';

try {
    $pdo = new PDO("mysql:host=$host;dbname=$dbname;charset=utf8mb4", $user, $password);
    $pdo->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);
} catch(PDOException $e) {
    die("Connection failed: " . $e->getMessage());
}

================================================================================
COMMAND LINE ACCESS:
================================================================================

mysql -u {{ item.db_user }} -p {{ item.db_name }}